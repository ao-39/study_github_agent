name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  ci:
    name: 継続的インテグレーション
    runs-on: ubuntu-latest
    permissions:
      issues: write
      pull-requests: write
      contents: read

    steps:
      - name: リポジトリのチェックアウト
        uses: actions/checkout@v4

      - name: Node.jsのセットアップ
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: pnpmのセットアップ
        uses: pnpm/action-setup@v4
        with:
          version: 10.11.0

      - name: pnpmストアディレクトリの取得
        shell: bash
        run: |
          echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV

      - name: pnpmキャッシュの設定
        uses: actions/cache@v4
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/package.json') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Turborepoキャッシュの設定
        uses: actions/cache@v4
        with:
          path: .turbo
          key: ${{ runner.os }}-turbo-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-turbo-

      - name: Playwrightブラウザキャッシュの設定
        uses: actions/cache@v4
        id: playwright-cache
        with:
          path: ~/.cache/ms-playwright
          key: ${{ runner.os }}-playwright-${{ hashFiles('**/package.json', '**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-playwright-

      - name: 依存関係のインストール
        run: pnpm install

      - name: リンティング
        run: pnpm lint

      - name: フォーマットチェック
        run: |
          pnpm exec turbo run lint:format
          pnpm run format:package --check

      - name: 型チェック
        run: pnpm type-check

      - name: ビルド
        run: pnpm build

      - name: ビルドファイル情報取得（PR時のみ）
        if: github.event_name == 'pull_request'
        id: build-info
        run: |
          cd apps/app
          # ビルドファイルの数を取得
          file_count=$(find dist -type f | wc -l)
          # 総サイズを取得
          total_size=$(du -sh dist/ | cut -f1)
          # 個別ファイルサイズを取得（サイズ順）
          file_details=$(find dist -type f -exec ls -lh {} \; | awk '{print $5 " " $9}' | sort -hr | head -10)
          
          echo "file_count=$file_count" >> $GITHUB_OUTPUT
          echo "total_size=$total_size" >> $GITHUB_OUTPUT
          echo "file_details<<EOF" >> $GITHUB_OUTPUT
          echo "$file_details" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: セキュリティ監査実行
        if: github.event_name == 'pull_request'
        id: audit
        run: |
          # pnpm auditの実行とJSON結果の取得（stderr出力を廃棄し、JSONのみ抽出）
          pnpm audit --json > audit_output.json 2>/dev/null || true
          if [ -s audit_output.json ] && jq empty audit_output.json 2>/dev/null; then
            audit_result=$(cat audit_output.json)
          else
            audit_result='{"metadata":{"vulnerabilities":{"info":0,"low":0,"moderate":0,"high":0,"critical":0},"dependencies":0,"devDependencies":0,"optionalDependencies":0,"totalDependencies":0},"advisories":{},"actions":[],"muted":[]}'
          fi
          echo "audit_result<<EOF" >> $GITHUB_OUTPUT
          echo "$audit_result" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT
          rm -f audit_output.json

      - name: 古いパッケージ調査実行
        if: github.event_name == 'pull_request'
        id: outdated
        run: |
          # pnpm outdatedの実行とJSON結果の取得（全ワークスペースを対象）
          pnpm outdated --format json --recursive > outdated_output.json 2>/dev/null || true
          if [ -s outdated_output.json ] && jq empty outdated_output.json 2>/dev/null; then
            outdated_result=$(cat outdated_output.json)
          else
            outdated_result='{}'
          fi
          echo "outdated_result<<EOF" >> $GITHUB_OUTPUT
          echo "$outdated_result" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT
          rm -f outdated_output.json

      - name: バンドル分析レポート生成（PR時のみ）
        if: github.event_name == 'pull_request'
        run: cd apps/app && ANALYZE=true pnpm vite build

      - name: ビルド成果物のArtifactsアップロード（PR時のみ）
        if: github.event_name == 'pull_request'
        uses: actions/upload-artifact@v4
        id: upload-artifacts
        with:
          name: build-artifacts-${{ github.event.pull_request.number }}
          path: apps/app/dist/
          retention-days: 14

      - name: バンドル分析レポートのArtifactsアップロード（PR時のみ）
        if: github.event_name == 'pull_request'
        uses: actions/upload-artifact@v4
        with:
          name: bundle-analysis-report-${{ github.event.pull_request.number }}
          path: apps/app/bundle-report.html
          retention-days: 14

      - name: PRにArtifactsダウンロードリンクをコメント（PR時のみ）
        if: github.event_name == 'pull_request'
        uses: peter-evans/create-or-update-comment@v4
        with:
          issue-number: ${{ github.event.pull_request.number }}
          body: |
            ## 🚀 ビルド成果物をダウンロード

            PR #${{ github.event.pull_request.number }} のビルド成果物が準備できました！

            **📦 ダウンロードはこちら👉** [GitHub Actionsの実行ページ](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }})

            **Artifactsセクションから以下のファイルをダウンロードできます：**
            - **ビルド成果物**: `build-artifacts-${{ github.event.pull_request.number }}`
              - **内容**: apps/appのビルド成果物（HTML、CSS、JavaScript）
            - **📊 Vitestテストレポート**: `vitest-html-report-${{ github.event.pull_request.number }}`
              - **内容**: Vitestユニットテストの詳細HTMLレポート
            - **📈 バンドル分析レポート**: `bundle-analysis-report-${{ github.event.pull_request.number }}`
              - **内容**: JavaScriptバンドルサイズの詳細分析（rollup-plugin-visualizer）
            - **🎭 Playwrightテストレポート**: `playwright-html-report-${{ github.event.pull_request.number }}`
              - **内容**: E2Eテスト（Playwright）の詳細HTMLレポート
            - **保存期間**: 14日間

            **📋 ダウンロード手順：**
            1. 上記のリンクをクリックしてActions実行ページを開く
            2. ページ下部の「Artifacts」セクションを確認
            3. 必要なファイルをダウンロード

            **📊 テストレポートについて：**
            - **Vitestレポート**: ユニットテストの実行結果、カバレッジ情報、実行時間、失敗詳細
            - **Playwrightレポート**: E2Eテストの実行結果、スクリーンショット、実行ログ、各ブラウザでの結果

            **📈 バンドル分析レポートについて：**
            バンドル分析レポートには以下の情報が含まれています：
            - JavaScriptファイルサイズの詳細分析
            - 依存関係のサイズ内訳（ライブラリ別）
            - ツリーマップによる視覚的なサイズ表示
            - gzip・brotli圧縮サイズの比較

            ---
            *このコメントは自動で生成されました。レビュー時にビルド成果物、テストレポート、バンドル分析レポートをご確認ください。*

      - name: セキュリティ監査結果を解析・準備
        if: github.event_name == 'pull_request'
        id: parse-audit
        run: |
          # audit結果からメタデータ情報を抽出
          total_deps=$(echo '${{ steps.audit.outputs.audit_result }}' | jq -r '.metadata.totalDependencies')
          critical=$(echo '${{ steps.audit.outputs.audit_result }}' | jq -r '.metadata.vulnerabilities.critical')
          high=$(echo '${{ steps.audit.outputs.audit_result }}' | jq -r '.metadata.vulnerabilities.high')
          moderate=$(echo '${{ steps.audit.outputs.audit_result }}' | jq -r '.metadata.vulnerabilities.moderate')
          low=$(echo '${{ steps.audit.outputs.audit_result }}' | jq -r '.metadata.vulnerabilities.low')
          info=$(echo '${{ steps.audit.outputs.audit_result }}' | jq -r '.metadata.vulnerabilities.info')
          
          total_vulns=$((critical + high + moderate + low + info))
          
          echo "total_deps=$total_deps" >> $GITHUB_OUTPUT
          echo "critical=$critical" >> $GITHUB_OUTPUT
          echo "high=$high" >> $GITHUB_OUTPUT
          echo "moderate=$moderate" >> $GITHUB_OUTPUT
          echo "low=$low" >> $GITHUB_OUTPUT
          echo "info=$info" >> $GITHUB_OUTPUT
          echo "total_vulns=$total_vulns" >> $GITHUB_OUTPUT
          
          # advisoriesが空でないかチェック
          has_advisories=$(echo '${{ steps.audit.outputs.audit_result }}' | jq -r 'if .advisories == {} then "false" else "true" end')
          echo "has_advisories=$has_advisories" >> $GITHUB_OUTPUT

      - name: 古いパッケージ結果を解析・準備
        if: github.event_name == 'pull_request'
        id: parse-outdated
        run: |
          # outdated結果が空でないかチェック
          has_outdated=$(echo '${{ steps.outdated.outputs.outdated_result }}' | jq -r 'if . == {} then "false" else "true" end')
          echo "has_outdated=$has_outdated" >> $GITHUB_OUTPUT
          
          if [ "$has_outdated" = "true" ]; then
            # 古いパッケージの数をカウント
            outdated_count=$(echo '${{ steps.outdated.outputs.outdated_result }}' | jq -r 'keys | length')
            echo "outdated_count=$outdated_count" >> $GITHUB_OUTPUT
            
            # パッケージ名一覧を取得（表示用）
            outdated_packages=$(echo '${{ steps.outdated.outputs.outdated_result }}' | jq -r 'keys | join(", ")')
            echo "outdated_packages=$outdated_packages" >> $GITHUB_OUTPUT
          else
            echo "outdated_count=0" >> $GITHUB_OUTPUT
            echo "outdated_packages=" >> $GITHUB_OUTPUT
          fi

      - name: セキュリティ監査結果をPRにコメント（PR時のみ）
        if: github.event_name == 'pull_request'
        uses: peter-evans/create-or-update-comment@v4
        with:
          issue-number: ${{ github.event.pull_request.number }}
          body: |
            ## 🔒 依存パッケージセキュリティ監査 & パッケージ更新状況

            PR #${{ github.event.pull_request.number }} のセキュリティ監査と依存パッケージの更新状況確認が完了しました！

            ### 📊 監査サマリー
            **📦 調査対象**: ${{ steps.parse-audit.outputs.total_deps }}件の依存パッケージ
            **🚨 検出された脆弱性**: ${{ steps.parse-audit.outputs.total_vulns }}件

            ### 📋 脆弱性レベル別詳細
            - 🔴 **Critical**: ${{ steps.parse-audit.outputs.critical }}件
            - 🟠 **High**: ${{ steps.parse-audit.outputs.high }}件  
            - 🟡 **Moderate**: ${{ steps.parse-audit.outputs.moderate }}件
            - 🔵 **Low**: ${{ steps.parse-audit.outputs.low }}件
            - ℹ️ **Info**: ${{ steps.parse-audit.outputs.info }}件

            ${{ steps.parse-audit.outputs.total_vulns == '0' && '✅ **セキュリティ問題は検出されませんでした！** 依存パッケージは安全です。' || '⚠️ **セキュリティ問題が検出されました。** 詳細は下記をご確認ください。' }}

            ### 📈 パッケージ更新状況
            **🔄 古いパッケージ**: ${{ steps.parse-outdated.outputs.outdated_count }}件

            ${{ steps.parse-outdated.outputs.outdated_count == '0' && '✅ **すべてのパッケージが最新です！** 更新が必要なパッケージはありません。' || '📦 **更新可能なパッケージがあります：** ' }}${{ steps.parse-outdated.outputs.outdated_count != '0' && steps.parse-outdated.outputs.outdated_packages || '' }}

            ${{ steps.parse-outdated.outputs.has_outdated == 'true' && '### 📋 古いパッケージの詳細

            <details>
            <summary>📄 詳細な更新情報（クリックで展開）</summary>

            ```json
            ' || '' }}${{ steps.parse-outdated.outputs.has_outdated == 'true' && steps.outdated.outputs.outdated_result || '' }}${{ steps.parse-outdated.outputs.has_outdated == 'true' && '```

            </details>

            ### 🛠️ パッケージ更新方法
            ```bash
            # 全パッケージの更新確認
            pnpm outdated

            # 特定のパッケージを更新
            pnpm update <package-name>

            # 全パッケージを安全に更新（セマンティックバージョニング範囲内）
            pnpm update
            ```' || '' }}

            ${{ steps.parse-audit.outputs.has_advisories == 'true' && '### 🔍 検出された脆弱性の詳細

            <details>
            <summary>📄 詳細なAdvisories情報（クリックで展開）</summary>

            ```json
            ' || '' }}${{ steps.parse-audit.outputs.has_advisories == 'true' && steps.audit.outputs.audit_result || '' }}${{ steps.parse-audit.outputs.has_advisories == 'true' && '```

            </details>

            ### 🛠️ 修復方法
            ```bash
            # 脆弱性を自動修復する場合
            pnpm audit --fix

            # 特定のCVEを無視する場合
            pnpm audit --ignore <CVE-ID>
            ```' || '' }}

            ### 💡 監査・更新チェックについて
            - **セキュリティ監査ツール**: pnpm audit
            - **パッケージ更新チェックツール**: pnpm outdated
            - **データソース**: npm security advisories, npm registry
            - **実行タイミング**: PR作成・更新時
            - **対象**: dependencies, devDependencies, optionalDependencies（全ワークスペース）

            ---
            *このセキュリティ監査・パッケージ更新状況レポートは自動で生成されました。重要な脆弱性が検出された場合は適切に対処し、古いパッケージがある場合は更新を検討してください。*

      - name: Vitestユニットテスト実行
        run: pnpm exec turbo run test -- --passWithNoTests

      - name: VitestテストJSON結果生成（PR時のみ）
        if: github.event_name == 'pull_request'
        id: vitest-json
        run: |
          cd apps/app
          # JSON結果を生成（カバレッジを無効にしてJSONのみ出力）
          pnpm exec vitest --reporter=json --run --silent --coverage=false > vitest-results.json 2>/dev/null || true
          if [ -s vitest-results.json ] && jq empty vitest-results.json 2>/dev/null; then
            vitest_result=$(cat vitest-results.json)
          else
            vitest_result='{"numTotalTests":0,"numPassedTests":0,"numFailedTests":0,"success":false,"testResults":[]}'
          fi
          echo "vitest_result<<EOF" >> $GITHUB_OUTPUT
          echo "$vitest_result" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT
          rm -f vitest-results.json

      - name: Vitestテストレポートの成果物アップロード（PR時のみ）
        if: github.event_name == 'pull_request'
        uses: actions/upload-artifact@v4
        with:
          name: vitest-html-report-${{ github.event.pull_request.number }}
          path: apps/app/test-results/
          retention-days: 14

      - name: Playwrightブラウザのインストール
        if: steps.playwright-cache.outputs.cache-hit != 'true'
        run: pnpm --filter app exec playwright install --with-deps chromium

      - name: E2Eテスト実行
        run: pnpm --filter app test:e2e:chromium

      - name: PlaywrightテストJSON結果生成（PR時のみ）
        if: always() && github.event_name == 'pull_request'
        id: playwright-json
        run: |
          cd apps/app
          # JSON結果を生成（エラーでも継続）
          timeout 120 pnpm exec playwright test --project=chromium --reporter=json > playwright-results.json 2>/dev/null || true
          if [ -s playwright-results.json ] && jq empty playwright-results.json 2>/dev/null; then
            playwright_result=$(cat playwright-results.json)
          else
            # Playwrightが失敗した場合のデフォルト結果
            playwright_result='{"stats":{"passed":0,"failed":0,"flaky":0,"skipped":0,"interrupted":0,"expected":0,"unexpected":0,"duration":0},"suites":[]}'
          fi
          echo "playwright_result<<EOF" >> $GITHUB_OUTPUT
          echo "$playwright_result" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT
          rm -f playwright-results.json

      - name: テスト結果サマリー解析（PR時のみ）
        if: github.event_name == 'pull_request'
        id: test-summary
        run: |
          # Vitestテスト結果の解析
          vitest_total=$(echo '${{ steps.vitest-json.outputs.vitest_result }}' | jq -r '.numTotalTests // 0')
          vitest_passed=$(echo '${{ steps.vitest-json.outputs.vitest_result }}' | jq -r '.numPassedTests // 0')
          vitest_failed=$(echo '${{ steps.vitest-json.outputs.vitest_result }}' | jq -r '.numFailedTests // 0')
          vitest_success=$(echo '${{ steps.vitest-json.outputs.vitest_result }}' | jq -r '.success // false')
          
          # 失敗したテストの詳細を取得
          vitest_failed_tests=""
          if [ "$vitest_failed" -gt 0 ]; then
            vitest_failed_tests=$(echo '${{ steps.vitest-json.outputs.vitest_result }}' | jq -r '.testResults[] | select(.status == "failed") | .assertionResults[] | select(.status == "failed") | "- " + .fullName')
          fi
          
          # Playwrightテスト結果の解析
          playwright_passed=$(echo '${{ steps.playwright-json.outputs.playwright_result }}' | jq -r '.stats.passed // 0')
          playwright_failed=$(echo '${{ steps.playwright-json.outputs.playwright_result }}' | jq -r '.stats.failed // 0')
          playwright_flaky=$(echo '${{ steps.playwright-json.outputs.playwright_result }}' | jq -r '.stats.flaky // 0')
          playwright_skipped=$(echo '${{ steps.playwright-json.outputs.playwright_result }}' | jq -r '.stats.skipped // 0')
          playwright_total=$((playwright_passed + playwright_failed + playwright_flaky + playwright_skipped))
          
          # 失敗したE2Eテストの詳細を取得
          playwright_failed_tests=""
          if [ "$playwright_failed" -gt 0 ]; then
            playwright_failed_tests=$(echo '${{ steps.playwright-json.outputs.playwright_result }}' | jq -r '.suites[]?.specs[]?.tests[]? | select(.status == "failed") | "- " + .title')
          fi
          
          echo "vitest_total=$vitest_total" >> $GITHUB_OUTPUT
          echo "vitest_passed=$vitest_passed" >> $GITHUB_OUTPUT
          echo "vitest_failed=$vitest_failed" >> $GITHUB_OUTPUT
          echo "vitest_success=$vitest_success" >> $GITHUB_OUTPUT
          echo "playwright_total=$playwright_total" >> $GITHUB_OUTPUT
          echo "playwright_passed=$playwright_passed" >> $GITHUB_OUTPUT
          echo "playwright_failed=$playwright_failed" >> $GITHUB_OUTPUT
          echo "playwright_flaky=$playwright_flaky" >> $GITHUB_OUTPUT
          echo "playwright_skipped=$playwright_skipped" >> $GITHUB_OUTPUT
          
          echo "vitest_failed_tests<<EOF" >> $GITHUB_OUTPUT
          echo "$vitest_failed_tests" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT
          
          echo "playwright_failed_tests<<EOF" >> $GITHUB_OUTPUT
          echo "$playwright_failed_tests" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: テスト結果サマリーをPRにコメント（PR時のみ）
        if: github.event_name == 'pull_request'
        uses: peter-evans/create-or-update-comment@v4
        with:
          issue-number: ${{ github.event.pull_request.number }}
          body: |
            ## 🧪 テスト結果サマリー

            PR #${{ github.event.pull_request.number }} のテスト実行が完了しました！

            ### 📊 単体テスト（Vitest）
            **📝 実行結果**: ${{ steps.test-summary.outputs.vitest_total }}件のテストを実行
            - ✅ **成功**: ${{ steps.test-summary.outputs.vitest_passed }}件
            - ❌ **失敗**: ${{ steps.test-summary.outputs.vitest_failed }}件
            - 🎯 **全体**: ${{ steps.test-summary.outputs.vitest_success == 'true' && '✅ すべてのテストが成功しました！' || '❌ 一部のテストが失敗しています' }}

            ${{ steps.test-summary.outputs.vitest_failed != '0' && '#### ❌ 失敗した単体テスト:
            ' || '' }}${{ steps.test-summary.outputs.vitest_failed != '0' && steps.test-summary.outputs.vitest_failed_tests || '' }}

            ### 🎭 E2Eテスト（Playwright）
            **📝 実行結果**: ${{ steps.test-summary.outputs.playwright_total }}件のテストを実行
            - ✅ **成功**: ${{ steps.test-summary.outputs.playwright_passed }}件
            - ❌ **失敗**: ${{ steps.test-summary.outputs.playwright_failed }}件
            - 🔄 **Flaky**: ${{ steps.test-summary.outputs.playwright_flaky }}件
            - ⏭️ **スキップ**: ${{ steps.test-summary.outputs.playwright_skipped }}件
            - 🎯 **全体**: ${{ steps.test-summary.outputs.playwright_failed == '0' && '✅ すべてのE2Eテストが成功しました！' || '❌ 一部のE2Eテストが失敗しています' }}

            ${{ steps.test-summary.outputs.playwright_failed != '0' && '#### ❌ 失敗したE2Eテスト:
            ' || '' }}${{ steps.test-summary.outputs.playwright_failed != '0' && steps.test-summary.outputs.playwright_failed_tests || '' }}

            ### 📦 ビルドファイル情報
            **📁 ファイル数**: ${{ steps.build-info.outputs.file_count }}件
            **📏 総サイズ**: ${{ steps.build-info.outputs.total_size }}

            #### 📄 ファイル詳細（サイズ順）
            ```
            ${{ steps.build-info.outputs.file_details }}
            ```

            ### 📋 詳細レポート
            詳細なテストレポートは以下からダウンロードできます：
            - **📊 Vitestテストレポート**: `vitest-html-report-${{ github.event.pull_request.number }}`
            - **🎭 Playwrightテストレポート**: `playwright-html-report-${{ github.event.pull_request.number }}`
            - **📦 ビルド成果物**: `build-artifacts-${{ github.event.pull_request.number }}`

            **📥 ダウンロード方法**: [GitHub Actionsの実行ページ](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}) の「Artifacts」セクションから各レポートをダウンロードしてください。

            ---
            *このテスト結果サマリーは自動で生成されました。*

      - name: PlaywrightテストレポートのArtifactsアップロード（PR時のみ）
        if: always() && github.event_name == 'pull_request'
        uses: actions/upload-artifact@v4
        with:
          name: playwright-html-report-${{ github.event.pull_request.number }}
          path: apps/app/playwright-report/
          retention-days: 14